var PostsPivoterViewModel = (function () {
    function PostsPivoterViewModel(elementId, preload) {
        this.elementId = elementId;
        alert(elementId);
    }
    return PostsPivoterViewModel;
}());
